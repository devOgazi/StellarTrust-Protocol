// Seed initial trusted issuers into the registry contract — placeholder.
import { StellarTrust } from '../sdk/src';

async function main() {
  console.log('Seeding issuers — implement with deployed contract IDs.');
}

main().catch(console.error);
